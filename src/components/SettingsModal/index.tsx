import React from 'react';
import { View, Text } from 'react-native';
import { styles } from './_layout';

const SaveMetering = () => {
    return (
        <View style={styles.container}>
            <Text style={styles.title}>Save Metering Screen</Text>
        </View>
    );
};

export default SaveMetering;
